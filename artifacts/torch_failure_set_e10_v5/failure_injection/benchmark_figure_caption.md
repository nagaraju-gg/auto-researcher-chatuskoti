Figure caption:

`Vec3` versus binary evaluation on the canonical four-case failure benchmark. A metric-only binary controller would adopt `3` of `4` cases, including the pyrrhic, Goodhart-style, and incomparable cases. `Vec3` instead routes those cases to `hold`, `reject`, and `reframe`, and escalates the damaged failure case to `rollback`.
