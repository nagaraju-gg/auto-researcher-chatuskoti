[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "catuskoti-autoresearcher"
version = "0.1.0"
description = "Deterministic V1 prototype for Catuskoti-style automated research evaluation."
requires-python = ">=3.11"

